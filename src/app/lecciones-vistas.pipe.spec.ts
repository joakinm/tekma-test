import { LeccionesVistasPipe } from './lecciones-vistas.pipe';

describe('LeccionesVistasPipe', () => {
  it('create an instance', () => {
    const pipe = new LeccionesVistasPipe();
    expect(pipe).toBeTruthy();
  });
});
